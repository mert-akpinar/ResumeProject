@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_DefaultLayout.cshtml";
}

    <!-- Services banner -->
    @Html.Partial("PartialBanner")
    <!-- //Services banner -->
    <!-- Services Section -->
    
    <!-- //Services Section -->
    @Html.Partial("PartialProject")
